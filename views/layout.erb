<!doctype html>
<html lang="en">
<title>Out Lock<%= if locals[:id] then " - #{id}" else "" end%></title>

<style>
  html {
    background-color: #ddd;
  } 
  body {
    font-family: sans-serif;
    background-color: #fff;
    margin: .5rem;
    padding: 1.5rem;
    border: 1px solid;
    position: relative;
  }
  h3{
    padding-bottom: 1rem;
    border-bottom: 1px solid black;
  }
  #logo {
    height: 10rem;
    width: auto;
  }
  #source-link{
    position: absolute;
    top: 1rem;
    right: 1rem;
  }
  code {
    background-color: #ddd;
    padding: .1rem .4rem;
    border-radius: .4rem;
  }
  .example {
    padding-left: 1rem;
  }

  .example > pre{
    background-color: #ddd;
    padding: .5rem .5rem;
    border-radius: .4rem;
    white-space: pre-line;
    border-left: .2rem solid #a7bed3;
  }
  .api {
    margin-bottom: 2rem;
  }
  .api-name {
    background-color: #fcf7de;
    padding: .5rem;
    border-radius: .5rem;
  }
  .api-description {
    padding: 1rem;
  }
  dt {
    font-weight: bold;
  }
</style>


<%= yield %>
</html>
