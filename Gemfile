# frozen_string_literal: true

source 'https://rubygems.org'

gem 'sinatra', '2.2.4'
gem 'sinatra-contrib', '2.2.4'
gem 'sqlite3', '1.4.4'
gem 'uuidtools', '2.2'

group :development do
  # run `rerun 'ruby outlock.rb'` for hot reloads in dev
  gem 'rack-test', '2.1'
  gem 'rerun', '~> 0.14.0'
  gem 'test-unit', '3.5.7'
end

# gem install rubocop-ast -v 1.17.0 #, require: false
# gem install rubocop -v 1.28.2
